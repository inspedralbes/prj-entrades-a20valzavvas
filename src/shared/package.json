{
  "name": "shared",
  "version": "0.0.1",
  "description": "Shared TypeScript types for Sala Onirica",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^3.2.0"
  }
}
